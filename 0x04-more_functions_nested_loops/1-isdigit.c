#include "main.h"

/**
 *  * _isdigit - Checks if a character is a digit
 *   * @x: The number will  be checked
 *    *
 *     * Return: 1 if character is a digit or else  0
 *      */
int _isdigit(int x)
{
		if (x >= 48 && x <= 57)
				{
							return (1);
								}

			return (0);
}
